name: Build Magic GL.iNet 4.x Firmware for JDCloud-1

on:
  repository_dispatch:
  workflow_dispatch:
    inputs:
      ssh:
        description: 'SSH connection to Actions'
        required: false
        default: 'false'

env:
  # 💡 已经帮你修正为官方构建器地址，如有私人魔改仓可自行替换此链接
  REPO_URL: https://github.com
  REPO_BRANCH: main
  CONFIG_FILE: .config
  DIY_SH: diy.sh

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Initialization environment
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        sudo rm -rf /etc/apt/sources.list.d/* /usr/share/dotnet /usr/local/lib/android /opt/ghc
        sudo -E apt-get -qq update
        sudo -E apt-get -qq install -y build-essential clang flex bison g++ gawk gcc-multilib g++-multilib gettext git libncurses5-dev libssl-dev patch unzip zlib1g-dev gcc-9 g++-9
        sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-9 100 --slave /usr/bin/g++ g++ /usr/bin/g++-9
        sudo -E apt-get -qq autoremove --purge
        sudo -E apt-get -qq clean
        sudo timedatectl set-timezone "Asia/Shanghai"

    - name: Clone source code
      run: |
        git clone -b $REPO_BRANCH $REPO_URL openwrt
        cd openwrt
        if [ -f setup.py ]; then
          python3 setup.py -m mt7621 || true
        fi

    - name: 魔改硬件引脚和设备树 (DTS)
      run: |
        # 兼容性路径检测，防止因目录结构变更导致 cd 失败
        if [ -d "openwrt/openwrt-21.02" ]; then
          TARGET_DIR="openwrt/openwrt-21.02"
        else
          TARGET_DIR="openwrt"
        fi
        
        DTS_PATH="${TARGET_DIR}/target/linux/ramips/dts/mt7621_glinet_gl-mt1300.dts"
        if [ -f "$DTS_PATH" ]; then
          sed -i 's/phy-handle = <&ethphy0>/phy-handle = <&ethphy4>/g' "$DTS_PATH"
        else
          echo "Warning: DTS file not found at $DTS_PATH"
        fi

    - name: 注入固件减肥配方 (精简体积至 32MB 内)
      run: |
        if [ -d "openwrt/openwrt-21.02" ]; then
          cd openwrt/openwrt-21.02
        else
          cd openwrt
        fi
        
        # 清理并重新建立 .config，防止历史配置污染
        rm -f .config
        touch .config
        
        # 注入你的个性化精简与保留配置
        echo "CONFIG_TARGET_ramips=y" >> .config
        echo "CONFIG_TARGET_ramips_mt7621=y" >> .config
        echo "CONFIG_TARGET_ramips_mt7621_DEVICE_glinet_gl-mt1300=y" >> .config
        echo "CONFIG_PACKAGE_gl-cloud-component=n" >> .config
        echo "CONFIG_PACKAGE_luci-app-sqm=n" >> .config
        echo "CONFIG_PACKAGE_luci-app-openvpn=n" >> .config
        echo "CONFIG_PACKAGE_luci-app-wireguard=y" >> .config
        echo "CONFIG_PACKAGE_luci-i18n-base-zh-cn=y" >> .config
        
        # 统一刷新配置补充依赖
        make defconfig

    - name: Download package
      run: |
        if [ -d "openwrt/openwrt-21.02" ]; then
          cd openwrt/openwrt-21.02
        else
          cd openwrt
        fi
        make download -j8
        find dl -size -1024c -exec ls -l {} \;
        find dl -size -1024c -exec rm -f {} \;

    - name: Compile the firmware
      run: |
        if [ -d "openwrt/openwrt-21.02" ]; then
          cd openwrt/openwrt-21.02
        else
          cd openwrt
        fi
        echo -e "$(nproc) thread compile"
        make -j$(nproc) || make -j1 || make -j1 V=s

    - name: Upload firmware to Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: JDCloud-1-GLiNet-4.x-Firmware
        path: |
          openwrt/openwrt-21.02/bin/targets/ramips/mt7621/*sysupgrade.bin
          openwrt/bin/targets/ramips/mt7621/*sysupgrade.bin
        if-no-files-found: warn
